[package]
name = "fuguquest"
version = "1.0.0"
edition = "2021"
description = "DirtyFrag-LPE orchestrator (Quest 3 / 3S / Pro / 2) — single OS-independent binary, no clang, no deps"

[[bin]]
name = "fuguquest"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
strip = true
