# Python deps to RUN the exploit (orchestrate.py).  Install:  python -m pip install -r requirements.txt
#   (use `python -m pip` so it lands in the same interpreter that runs `python orchestrate.py`)
#
# Provides Crypto.Cipher.AES (the Dirty-Frag keystream). This is the ONLY Python dep for running.
pycryptodome>=3.15

# To ADD a new firmware target (port.py) you also need vmlinux-to-elf — see requirements-port.txt.
# It is intentionally NOT here: its minilzo sub-dep needs a C compiler on Windows, which would break
# `pip install -r requirements.txt` for people who only want to run the exploit.
#
# Non-pip tools (see SETUP.md): adb (platform-tools), LLVM/clang, and for port.py also
# payload-dumper-go + debugfs.
