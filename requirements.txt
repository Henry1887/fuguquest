# RUN the exploit (orchestrate.py): NO required packages.
# orchestrate.py has a built-in, self-contained pure-Python AES-128 fallback (verified vs
# pycryptodome + FIPS-197), so it runs on any Python with zero third-party deps — no pip install,
# no C compiler. `pip install -r requirements.txt` here is intentionally a no-op.
#
# OPTIONAL speedup (uses the C-accelerated AES instead of the pure fallback — a few seconds faster
# per run). Only install if you already have wheels/a compiler; skip it on bleeding-edge Pythons:
#   python -m pip install pycryptodome
#
# To ADD a new firmware target (port.py) you need vmlinux-to-elf — see requirements-port.txt.
# Non-pip tools (SETUP.md): adb, LLVM/clang, and for port.py also payload-dumper-go + debugfs.
